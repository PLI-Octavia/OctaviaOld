// Generated by LiveScript 1.5.0
var $eye, PREFIXES;
$eye = $('.eye');
if ($eye.length) {
  PREFIXES = ['-webkit-', '-moz-', '-ms-', ''];
  $(document.body).mousemove(function(it){
    var ref$, left, top, x, y, rad, rot;
    ref$ = $eye.offset(), left = ref$.left, top = ref$.top;
    x = left + $eye.width() / 2;
    y = top + $eye.height() / 2;
    rad = Math.atan2(it.pageX - x, it.pageY - y);
    rot = rad * (180 / Math.PI) * -1 + 230;
    $eye.css((function(){
      var i$, x$, ref$, len$, resultObj$ = {};
      for (i$ = 0, len$ = (ref$ = PREFIXES).length; i$ < len$; ++i$) {
        x$ = ref$[i$];
        resultObj$[x$ + "transform"] = "rotate(" + rot + "deg)";
      }
      return resultObj$;
    }()));
  });
}