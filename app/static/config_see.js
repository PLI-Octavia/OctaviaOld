// Generated by LiveScript 1.5.0
octavia.register('config_see', function(){
  var save, handler;
  if (typeof formConfig == 'undefined' || formConfig === null) {
    throw new Error("no form config");
  }
  if (typeof currentConfig == 'undefined' || currentConfig === null) {
    throw new Error("no current config");
  }
  save = function(){
    gEBI('inputconf').value = JSON.stringify(currentConfig);
  };
  handler = function(key, value){
    currentConfig[key] = value;
    return save();
  };
  renderFormTo(gEBI('settings-form'), formConfig, currentConfig, handler);
  save();
});